// Asset-type category codes (mirrors the legend on the reference site).
export const CATEGORY_CODES = ['BRD', 'CS', 'DV', 'MV', 'SD', 'WP', 'WU', 'O'];

// Maturity / lifecycle stage of an agent (the red "#Prototype" style tags).
export const STAGES = ['Prototype', 'POV', 'MVP', 'Production'];

// Availability status shown as the animated badge on each agent card.
export const STATUSES = ['Active', 'Upcoming'];
